# AGENTS.md - 通用 AI 编码规则模板

> **适用对象**：AI 编程助手（如 Antigravity、Cursor、Claude Code 等）  
> **版本**：v1.0  
> **目的**：约束 AI 行为，确保生成的代码符合项目架构与业务逻辑，避免“勤奋但错误”的修改。

---

## 1. 项目背景速览（请根据实际填写）

- **项目名称**：[填写你的项目名称]  
- **核心功能**：[简述项目主要功能，如“工作空间管理中枢”]  
- **技术栈**：[例如：React + TypeScript + Vite + Material-UI]  
- **状态管理**：[例如：Redux Toolkit / Zustand / Pinia]  
- **API 层**：[例如：`/src/api`，基于 axios，统一拦截]  
- **设计规范**：[例如：主色 #1976d2，背景 #f5f5f5，卡片圆角 8px]  


## 2. 四条核心编码原则（Karpathy）

### ① 编码前先思考（Think Before Coding）
- **每次收到需求时，必须先输出“理解确认”**，包括：
  - 我理解的任务目标是什么？
  - 我的假设（尤其是关于数据、权限、接口的假设）有哪些？
  - 需求中哪些地方不明确，需要澄清？
- **在有疑问时主动提问，不要猜测**。例如：不确定 API 端点时，应先询问。
- **展示设计方案（如有设计决策）**，例如：“我打算用 [UI 组件] 并配合 [自定义 Hook]，你是否接受？”

### ② 简单优先（Simplicity First）
- **坚持“最小实现”**：只写为完成当前任务所必需的代码，**不添加**：
  - 额外的配置项
  - 未被要求的抽象层或设计模式
  - 兼容未来需求的“预留”代码
  - 额外的注释（除非必要）
- **优先使用项目现有的模式**（如已有的组件结构、命名规范），不要发明新风格。
- **如果某个实现方法需要超过 30 行新增代码**，请先思考是否有更简单的方式。

### ③ 外科手术式修改（Surgical Changes）
- **只修改与当前任务直接相关的文件和方法**。禁止：
  - “顺手”重命名变量
  - “顺便”调整无关代码的格式或缩进
  - 修改与本次修改无关的现有注释
- **如果必须调整现有代码（如重构）**，先明确说明理由并获得确认。
- **提交修改前，对照需求检查**：是否只改动了必要部分？

### ④ 目标驱动执行（Goal-Driven Execution）
- **每次任务必须包含“如何验证”**。在生成代码前，先列出验收标准：
  - 例如：“修改后，点击编辑按钮应弹出模态框，保存成功后列表应刷新”
- **主动进行本地逻辑验证**（在不启动完整项目的情况下，检查关键逻辑是否正确）。
- **如果 API 或外部依赖不可用，使用 mock 数据，并在代码中标注 `// TODO: 替换为真实接口`**。

---

## 3. 业务规则强制约束（请根据项目自定义）

> 此部分用于约束 AI 在开发时遵循的业务逻辑，避免错误。请根据实际业务填写。

### 3.1 权限规则（示例）
- **前端所有操作按钮对所有人可见**（不根据角色隐藏）。
- **操作时由后端校验角色权限**，前端根据后端返回的 `403` 或错误码显示提示。
- **角色与操作对照**（仅供参考，后端强制校验）：
  - [角色A]：所有操作
  - [角色B]：除[敏感操作]外的所有操作
  - [角色C]：仅查看

### 3.2 关键业务流程（示例）
- **升级/迁移操作**：需先触发二次身份验证，获得安全凭证后再调用危险 API。
- **数据刷新**：任何写操作成功后，刷新对应的列表（而非整页），并显示成功 Toast。
- **错误处理**：必须显示后端返回的具体错误信息，不得静默失败。

### 3.3 模态框/弹窗管理（示例）
- **同一时刻只能打开一个模态框**（使用全局状态控制）。
- **打开模态框时禁止背景滚动**。
- **高风险操作**（删除、注销、清空等）必须二次确认或身份验证。

---

## 4. 常用 API 接口（请根据实际后端填写）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/xxx` | GET | 获取xxx数据 |
| `/api/xxx` | POST | 创建xxx |
| `/api/xxx` | PATCH | 更新xxx |
| `/api/xxx` | DELETE | 删除xxx（需携带 `security_token`） |

> **重要**：所有 API 调用必须使用项目统一的 axios 实例，并统一处理错误。

---

## 5. 交互与 UI 约束（请根据设计系统填写）

- **布局**：[例如：固定左8/右4 双列 Bento 布局]
- **圆角**：卡片 `8px`，按钮 `4px`。
- **颜色**：主色 `[#xxx]`，背景 `[#xxx]`，警告色 `[#xxx]`，危险色 `[#xxx]`。
- **UI 组件库**：[例如：Ant Design Vue 4.x / Material-UI / Element Plus]
- **图标库**：[例如：@ant-design/icons-vue / @mui/icons-material]

---

## 6. 编码风格（自动执行）

- **语言与框架**：[例如：TypeScript + React / Vue 3]
- **组件命名**：`PascalCase`，文件名为 `kebab-case`（或 `PascalCase`，统一即可）。
- **状态管理**：使用 [`useXxxStore` / `useXxx`] 模式。
- **API 函数**：返回 `Promise`，统一处理错误。
- **禁止使用 `any`**，尽量使用 `interface` 定义数据结构。

---

## 7. 验证清单（AI 自检）

在提交代码前，AI 应逐条检查：
- [ ] 所有新增/修改的代码是否只服务于当前任务？
- [ ] 是否避免了不必要的抽象或未来兼容代码？
- [ ] 权限逻辑是否只由后端控制，前端只做展示和提示？
- [ ] 写操作后是否正确刷新了列表而非整页？
- [ ] 高风险操作是否绑定了二次确认/身份验证？
- [ ] 模态框是否实现了单例和背景滚动禁用？
- [ ] 是否添加了必要的错误提示？
- [ ] 是否使用了正确的组件库 API？
- [ ] 是否有遗留的 `console.log` 或调试代码？

---

**最后提醒**：当你不确定时，**先提问，再编码**。宁可多一轮对话，也不要生成错误的代码。

---